/* global window */

'use strict';

/**
 * BeaconsQueue
 *
 * @class BeaconsQueue
 */

var BeaconsQueue;
var debug = require('debug')('BeaconsQueue');

BeaconsQueue = {
    _beaconsQueue = [],

    _waitBeaconsCompleted: function () {
    },

    fireBeacon: function () {
    }
}

module.exports = BeaconsQueue;
